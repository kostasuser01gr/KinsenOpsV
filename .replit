run = "flutter run -d web-server --web-port=3000 --web-hostname=0.0.0.0"

[env]
PATH = "/home/runner/${REPL_SLUG}/flutter/bin:${PATH}"
FLUTTER_ROOT = "/home/runner/${REPL_SLUG}/flutter"

[nix]
channel = "stable-23_11"
